## Why

What user problem does this PR solve?

## What changed

- 

## Validation

- [ ] `bash -n` passed for touched shell files
- [ ] `shellcheck` passed for touched shell files
- [ ] `./scripts/build-bundle.sh` run (if `scripts/lib/*`, `setup`, or templates changed)
- [ ] `./scripts/verify-readme-assets.sh` passed (if README/screenshots changed)

## Risk and rollback

Potential risks and how to revert safely.
